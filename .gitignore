# Compiled binary (rebuild with `make`)
game

# Dev screenshots (from `./game --shot out.png`)
preview.png
crop.png

# Demo output (from `./make-video.sh` / `./game --record <dir>`)
*.mp4
frame_*.png

# macOS clutter
.DS_Store
